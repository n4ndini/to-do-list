import { UserID, User, DataStore } from "../../types";
import { getData } from "../../dataStore";

export function create_user(email: string, password: string,
                            firstName: string, lastName: string):number {

    // write function here

return 1;
}